# ЗВІТ

## Лабораторна робота №7

### Тема: Синхронізація даних між мобільним пристроєм, локальною БД та хмарою в умовах перервистого з'єднання

**Варіант:** 12  
**Тема індивідуального завдання:** Система моніторингу паркування автомобілів  
**Мета:** Розробити прототип мобільної смарт-системи, яка генерує емульовані дані сенсорів, обробляє їх у мобільному додатку з візуалізацією та управлінням, синхронізує з хмарним сервісом для зберігання та аналізу, та реалізує автоматичне керування виконавцями на основі отриманих даних.

---

## 1. ВСТУП

У рамках даної лабораторної роботи була розроблена система моніторингу паркування автомобілів для багатоповерхового гаража. Система включає генератор емульованих даних сенсорів, мобільний додаток на платформі Android, інтеграцію з хмарним сервісом Firebase Firestore для синхронізації даних, а також механізм автоматичного керування пристроями на основі отриманих даних сенсорів.

Система демонструє повний цикл від отримання даних від датчиків до виконання дій через автоматизовані правила, забезпечуючи при цьому роботу в умовах перервистого з'єднання завдяки локальному кешуванню та автоматичній синхронізації при відновленні зв'язку.

---

## 2. ОПИС СИСТЕМИ

Система моніторингу паркування складається з чотирьох основних компонентів:

1. **Компонент 1:** Генератор емульованих даних сенсорів (REST API сервер на Python Flask)
2. **Компонент 2:** Хмарний сервіс (Firebase Firestore)
3. **Компонент 3:** Емулятор керування пристроями (інтегрований з REST API)
4. **Компонент 4:** Мобільний додаток (Android)

Архітектура системи забезпечує:

- Генерацію реалістичних даних сенсорів з трендами та аномаліями
- Збереження даних локально в SQLite через Room
- Синхронізацію з хмарним сервісом Firebase Firestore
- Автоматичне керування пристроями на основі правил
- Роботу в офлайн-режимі з подальшою синхронізацією

---

## 3. РЕАЛІЗАЦІЯ КОМПОНЕНТІВ

### 3.1. Компонент 1: Генератор емульованих даних сенсорів

**Технології:** Python 3, Flask, Flask-CORS, Firebase Admin SDK

**Опис реалізації:**

Генератор емульованих даних реалізований як REST API сервер на Python Flask, який запускається окремо від мобільного додатку. Сервер розгорнуто на платформі Railway за адресою: `https://web-production-ca790.up.railway.app/`

**Функціональність:**

1. **Генерація реалістичних даних:**

   - Датчики наявності авто на місцях: масив з 100 значень (0 = вільне місце, 1 = зайняте)
   - Кількість вільних місць: 0-100 (динамічно змінюється)
   - Якість повітря (CO): 0-500 ppm (залежить від кількості зайнятих місць)
   - Якість повітря (NOx): 0-500 ppm (корелює з CO)
   - Температура: 5-10°C з реалістичними добовими циклами

2. **Реалістична поведінка:**

   - Логічні зв'язки між сенсорами: кількість машин впливає на рівень CO і NOx
   - Добові цикли температури (ніч прохолодніше, день тепліше)
   - Поступові зміни (тренди) замість випадкових стрибків
   - Додавання шуму для реалістичності
   - Випадкові аномалії (5% ймовірність для CO)

3. **API Endpoints:**
   - `GET /api/sensor-data` - отримати поточні дані сенсорів
   - `GET /api/sensor-data/stream` - потокова передача через Server-Sent Events (SSE)
   - `GET /api/health` - перевірка стану сервера

**Приклад відповіді API:**

```json
{
  "timestamp": 1704067200000,
  "parking_sensors": [0,1,0,1,1,...],
  "parking_occupied": 0.5,
  "free_spots": 50,
  "co_level": 120.5,
  "nox_level": 80.3,
  "temperature": 7.2
}
```

**Особливості реалізації:**

- Використання стану для збереження трендів між запитами
- Генерація даних на кожен запит з урахуванням попереднього стану
- Детальне логування всіх запитів для діагностики
- Підтримка CORS для доступу з мобільного додатку

**Деплой на Railway:**

Сервер розгорнуто на платформі Railway за адресою: **https://web-production-ca790.up.railway.app/**

**Особливості деплою:**

- Автоматичний деплой з GitHub репозиторію при кожному push
- Використання змінної середовища `PORT` для динамічного призначення порту
- Опціональна інтеграція з Firebase через змінну `FIREBASE_CREDENTIALS` (JSON у вигляді рядка)
- Автоматичне перезапущення сервера при змінах коду
- Логування всіх запитів для моніторингу та діагностики

**Переваги хмарного деплою:**

- Доступність 24/7 з будь-якого пристрою з інтернетом
- Не потрібно запускати локально для тестування мобільного додатку
- Автоматичне масштабування та відновлення при збоях
- Просте оновлення через Git push

**Використання:**
Мобільний додаток налаштований на використання хмарного API за замовчуванням. Це дозволяє тестувати додаток на реальних Android пристроях без необхідності локального запуску сервера або налаштування мережевого доступу.

---

### 3.2. Компонент 2: Хмарний сервіс (Firebase Firestore)

**Технології:** Firebase Firestore, Firebase Admin SDK (Python), Firebase SDK (Android)

**Опис реалізації:**

Для синхронізації даних між пристроями та зберігання в хмарі використовується Firebase Firestore. Структура даних організована в три колекції:

1. **`sensor_data`** - дані сенсорів

   - Документ ID: timestamp (мілісекунди)
   - Поля: timestamp, free_spots, co_level, nox_level, temperature, parking_sensors, parking_occupied

2. **`device_states`** - стани пристроїв

   - Документ ID: device_id
   - Поля: device_id, device_type, enabled, brightness/fan_speed/heating_power, last_updated

3. **`automation_rules`** - правила автоматизації
   - Документ ID: rule_id
   - Поля: rule*id, rule_name, enabled, sensor_type, operator, threshold, device_type, action*параметри

**Функціональність синхронізації:**

1. **Відвантаження даних (Upload):**

   - Пакетне відвантаження несинхронізованих записів
   - Використання batch операцій для ефективності
   - Відстеження синхронізованих записів через поле `synced`

2. **Завантаження даних (Download):**

   - Завантаження даних з Firebase з урахуванням останнього локального timestamp
   - Фільтрація дублікатів перед вставкою
   - Об'єднання локальних та хмарних даних

3. **Синхронізація станів пристроїв:**
   - Двостороння синхронізація станів пристроїв між API сервером та мобільним додатком
   - Збереження останніх станів пристроїв у Firebase

**Особливості:**

- Використання timestamp як унікального ідентифікатора для запобігання дублікатів
- Підтримка інкрементальної синхронізації (тільки нові дані)
- Обробка помилок та повторні спроби при збоях

---

### 3.3. Компонент 3: Емулятор керування пристроями

**Технології:** Python Flask, Firebase Admin SDK

**Опис реалізації:**

Емулятор керування пристроями інтегрований в REST API сервер (компонент 1) і надає API для отримання та зміни станів пристроїв.

**Типи пристроїв:**

1. **Панелі з напрямками (direction_panels_1):**

   - Параметри: enabled (bool), brightness (0-100)

2. **Вентиляція (ventilation_1):**

   - Параметри: enabled (bool), fan_speed (1-3)

3. **Система обігріву (heating_1):**
   - Параметри: enabled (bool), heating_power (1-2)

**API Endpoints:**

- `GET /api/devices` - отримати стан всіх пристроїв
- `GET /api/devices/{deviceId}` - отримати стан конкретного пристрою
- `PUT /api/devices/{deviceId}` - оновити стан пристрою

**Приклад запиту:**

```bash
PUT /api/devices/direction_panels_1
Content-Type: application/json

{
  "enabled": true,
  "brightness": 100
}
```

**Синхронізація з Firebase:**

- При кожній зміні стану пристрою через API, стан автоматично синхронізується з Firebase
- При старті сервера стани пристроїв завантажуються з Firebase (якщо вони там є)
- Це забезпечує синхронізацію станів між API сервером та мобільним додатком

**Особливості:**

- Валідація параметрів (діапазони значень)
- Логування всіх змін станів пристроїв
- Збереження timestamp останнього оновлення

---

### 3.4. Компонент 4: Мобільний додаток (Android)

**Технології:** Kotlin, Android SDK, Room Database, Firebase SDK, Retrofit, Coroutines, MPAndroidChart, Jetpack Navigation

**Опис реалізації:**

Мобільний додаток реалізовано на мові Kotlin з використанням сучасних Android технологій та архітектури MVVM.

#### 3.4.1. Архітектура додатку

**Архітектурні компоненти:**

- **ViewModel** - управління даними для UI (MonitoringViewModel, DeviceControlViewModel, RulesViewModel)
- **Repository** - централізований доступ до даних (ParkingRepository)
- **Room Database** - локальне зберігання даних
- **FirebaseService** - інтеграція з Firebase Firestore
- **ApiClient** - REST API клієнт для зв'язку з генератором даних

#### 3.4.2. Локальне зберігання (Room Database)

**Структура бази даних:**

1. **sensor_data** - дані сенсорів

   - Поля: id (Primary Key), timestamp, free_spots, co_level, nox_level, temperature, parking_sensors, parking_occupied, synced

2. **device_state** - поточні стани пристроїв

   - Поля: device_id (Primary Key), device_type, enabled, brightness, fan_speed, heating_power, last_updated, synced

3. **automation_rules** - правила автоматизації

   - Поля: rule*id (Primary Key), rule_name, enabled, sensor_type, operator, threshold, device_type, action*параметри, last_triggered, synced

4. **Історія пристроїв:** direction_panels_history, ventilation_history, heating_history

**Функціональність:**

- Автоматичне збереження всіх отриманих даних сенсорів
- Відстеження несинхронізованих записів через поле `synced`
- Потокові запити через Flow для реактивного оновлення UI

#### 3.4.3. Візуалізація даних

**Екран моніторингу:**

1. **Графіки в реальному часі:**

   - Графік вільних місць (лінійний графік)
   - Графік рівня CO (лінійний графік)
   - Графік температури (лінійний графік)
   - Використання бібліотеки MPAndroidChart

2. **Фільтрація за часовими проміжками:**

   - Останні 1 година
   - Останні 6 годин
   - Останні 24 години
   - Останні 7 днів
   - Останні 30 днів
   - Власний діапазон (вибір дати)

3. **Статистика:**

   - Кількість записів
   - Середнє значення (для кожного типу сенсора)
   - Медіана (для кожного типу сенсора)
   - Тренд (зростання/спад/стабільно)
   - Відображення індикаторів трендів (↑↓→)

4. **Відображення поточних значень:**
   - Поточна кількість вільних місць
   - Поточний рівень CO/NOx
   - Поточна температура
   - Візуальні індикатори (кольори для різних діапазонів)

#### 3.4.4. Керування пристроями

**Екран керування пристроями:**

1. **Панелі з напрямками:**

   - Перемикач увімкнено/вимкнено
   - Регулятор яскравості (0-100%)

2. **Вентиляція:**

   - Перемикач увімкнено/вимкнено
   - Регулятор швидкості (1-3)

3. **Система обігріву:**
   - Перемикач увімкнено/вимкнено
   - Регулятор потужності (1-2)

**Функціональність:**

- Отримання поточних станів пристроїв з API сервера
- Оновлення станів через API (PUT запити)
- Локальне кешування станів
- Візуальне відображення поточних станів
- Синхронізація з Firebase

#### 3.4.5. Правила автоматизації

**Екран правил автоматизації:**

Реалізовані правила за індивідуальним завданням:

1. **Правило 1:** Якщо вільних місць < 10 → включити панелі з напрямками

   - Сенсор: FREE_SPOTS
   - Оператор: LESS_THAN
   - Поріг: 10
   - Дія: включити панелі з яскравістю 100%

2. **Правило 2:** Якщо CO > 300 ppm → максимально включити вентиляцію

   - Сенсор: CO_LEVEL
   - Оператор: GREATER_THAN
   - Поріг: 300
   - Дія: включити вентиляцію на швидкості 3

3. **Правило 3:** Якщо T < -5°C → включити обігрів в'їзду
   - Сенсор: TEMPERATURE
   - Оператор: LESS_THAN
   - Поріг: -5
   - Дія: включити обігрів на потужності 2

**Функціональність:**

- Створення/редагування/видалення правил
- Увімкнення/вимкнення правил
- Зміна порогових значень
- Відображення часу останнього спрацювання
- Автоматична оцінка правил при отриманні нових даних сенсорів
- Синхронізація правил з Firebase

**Механізм оцінки правил:**

- При отриманні нових даних сенсорів автоматично перевіряються всі увімкнені правила
- Якщо умова правила виконується, виконується відповідна дія (зміна стану пристрою)
- Оновлюється timestamp останнього спрацювання правила

#### 3.4.6. Синхронізація даних

**Механізм синхронізації:**

1. **Автоматична синхронізація:**

   - При старті додатку (якщо є інтернет)
   - Періодично під час генерації даних (кожні ~1 хвилину)
   - При відновленні інтернету (виявлення через NetworkCallback)

2. **Ручна синхронізація:**

   - Кнопка "Синхронізувати" на екрані моніторингу
   - Двостороння синхронізація (відвантаження + завантаження)

3. **Процес синхронізації:**

   - **Відвантаження (Upload):**

     - Збір несинхронізованих записів з локальної БД (synced = false)
     - Пакетне відвантаження в Firebase
     - Позначення записів як синхронізованих

   - **Завантаження (Download):**
     - Визначення останнього локального timestamp
     - Завантаження нових даних з Firebase (timestamp > останній локальний)
     - Фільтрація дублікатів перед вставкою
     - Вставка нових записів в локальну БД з synced = true

4. **Обробка перервистого з'єднання:**
   - Локальне кешування всіх даних
   - Відстеження несинхронізованих записів
   - Автоматична спроба синхронізації при з'явленні інтернету
   - Відображення статусу з'єднання в UI (індикатор онлайн/офлайн)

**Статус з'єднання:**

- Відображення індикатора з'єднання (🟢 онлайн / 🔴 офлайн)
- Перевірка доступності Firebase та API сервера
- Автоматичне виявлення зміни стану з'єднання

#### 3.4.7. Офлайн-режим

**Функціональність:**

- Повна функціональність без інтернету (крім синхронізації)
- Перегляд історії даних з локальної БД
- Керування пристроями (через локальний контролер)
- Робота з правилами автоматизації
- Автоматична синхронізація при відновленні з'єднання

---

## 4. ОСОБЛИВОСТІ РЕАЛІЗАЦІЇ

### 4.1. Реалістична генерація даних

Генератор даних враховує логічні зв'язки між сенсорами:

- Кількість зайнятих місць впливає на рівень CO та NOx
- Температура має добові цикли з реалістичними значеннями
- Додавання шуму та випадкових аномалій для реалістичності

### 4.2. Оптимізація синхронізації

- Використання timestamp як унікального ідентифікатора
- Інкрементальна синхронізація (тільки нові дані)
- Пакетне відвантаження для ефективності
- Фільтрація дублікатів на різних рівнях

### 4.3. Обробка помилок

- Try-catch блоки для всіх мережевих операцій
- Fallback на локальний контролер при недоступності API
- Логування помилок для діагностики
- Graceful degradation при відсутності інтернету

### 4.4. Реактивність UI

- Використання Flow та StateFlow для реактивного оновлення UI
- Автоматичне оновлення графіків при отриманні нових даних
- Плавна анімація змін

---

## 5. ІНСТРУКЦІЯ З ЗАПУСКУ

### 5.1. Запуск API сервера

API сервер розгорнуто на Railway і доступний за адресою:
**https://web-production-ca790.up.railway.app/**

Для локального запуску:

1. Перейти в директорію `sensor-api-server/`
2. Встановити залежності: `pip install -r requirements_api.txt`
3. (Опціонально) Налаштувати Firebase credentials
4. Запустити сервер: `python sensor_api_server.py`

### 5.2. Налаштування Android додатку

1. Налаштувати Firebase:

   - Створити проект у Firebase Console
   - Додати Android додаток до проекту
   - Завантажити `google-services.json` в папку `app/`
   - Увімкнути Firestore Database

2. Налаштувати підключення до API:

   - Для емулятора: `BASE_URL = "http://10.0.2.2:5000/"`
   - Для реального пристрою: `BASE_URL = "http://YOUR_IP:5000/"`
   - Або використовувати деплой: `BASE_URL = "https://web-production-ca790.up.railway.app/"`

3. Запустити додаток

### 5.3. Використання додатку

1. **Екран моніторингу:**

   - Натиснути "Почати генерацію" для отримання даних
   - Вибрати часовий проміжок для відображення
   - Переглядати графіки та статистику
   - Натиснути "Синхронізувати" для синхронізації з хмарою

2. **Екран керування пристроями:**

   - Переглянути поточні стани пристроїв
   - Змінити стани пристроїв (увімкнути/вимкнути, змінити параметри)
   - Стани автоматично синхронізуються з API та Firebase

3. **Екран правил автоматизації:**
   - Переглянути список правил
   - Створити/редагувати/видалити правила
   - Змінити порогові значення
   - Увімкнути/вимкнути правила
   - Правила автоматично оцінюються при отриманні нових даних

---

## 6. ВИСНОВКИ

У рамках лабораторної роботи була успішно розроблена та реалізована система моніторингу паркування автомобілів, яка включає всі необхідні компоненти:

✅ **Компонент 1:** Реалізовано REST API сервер на Python Flask з реалістичною генерацією даних сенсорів, розгорнуто на Railway

✅ **Компонент 2:** Інтегровано Firebase Firestore для хмарного зберігання та синхронізації даних

✅ **Компонент 3:** Реалізовано API для керування емульованими пристроями з синхронізацією станів

✅ **Компонент 4:** Розроблено повнофункціональний Android додаток з:

- Візуалізацією даних в реальному часі (графіки, статистика)
- Локальним збереженням даних (Room/SQLite)
- Синхронізацією з хмарою (Firebase Firestore)
- Керуванням пристроями
- Автоматичними правилами керування
- Роботою в офлайн-режимі

Система демонструє повний цикл від отримання даних від сенсорів до виконання автоматизованих дій через правила, забезпечуючи при цьому надійну роботу в умовах перервистого з'єднання завдяки локальному кешуванню та автоматичній синхронізації при відновленні зв'язку.

**Особливості системи:**

- Реалістична генерація даних з логічними зв'язками
- Ефективна синхронізація з запобіганням дублікатів
- Надійна робота в офлайн-режимі
- Автоматичне керування пристроями на основі правил
- Сучасна архітектура Android додатку (MVVM, Room, Coroutines)
- Розгортання API сервера на хмарній платформі

Система готова до використання та може бути розширена додатковими функціями, такими як сповіщення, розширена аналітика, інтеграція з реальними датчиками тощо.

---

**Дата виконання:** ********\_********  
**Виконав:** ********\_********
